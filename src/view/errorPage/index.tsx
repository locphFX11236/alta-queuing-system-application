import { Typography } from "antd";

const ErrorPage = (): JSX.Element => (
    <Typography.Title className="title-lv-1" style={{ margin: 150 }}>Error, Page not found!</Typography.Title>
);

export default ErrorPage;